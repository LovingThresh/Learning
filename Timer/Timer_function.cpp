//
// Created by liuye on 2023/11/26.
//
#include <Windows.h>
unsigned nz_count = 0, nz_sum = 0;
UWORD last, next;
DWORD start = GetTickCount();

// DWORD startTime = GetTickCount();
// for (last = GetTickCount(); nz_count; last = next) {
    // next =
// }